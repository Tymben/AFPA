var n = parseInt(prompt("Saisissez la longueur du tableau"));
console.log(n)
var tab = new Array(n);
console.log(tab)

for (i = 0; i < tab.length; i++)
        tableau[i] = parseInt(prompt("Saisissez vos valeurs"));
        console.log(tab);

        if (n < i) {
                n == true
        }
                else {
                n == false
                }







alert("Le nombre à la plus grande valeur est" + " " + Math.max(...tab));
console.log(Math.max(...tab));